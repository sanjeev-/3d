# Core dependencies
pyyaml>=6.0
bpy==5.1.1  # Blender as a Python module; wheels are Python 3.13 only.
            # Must match the Blender build you render with.

# Video processing
ffmpeg-python>=0.2.0

# Utilities
colorama>=0.4.6  # Colored terminal output
tqdm>=4.65.0     # Progress bars
